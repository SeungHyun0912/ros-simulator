# 실행 결과
이 폴더의 결과 데이터는 Git에서 제외됩니다.
향후 실행기에서 `<run_id>/`별로 config_snapshot.yaml, events.jsonl, metrics.json,
junit.xml, rosbag 등을 기록하세요. 현재 템플릿이 자동으로 이 파일들을 생성하지는 않습니다.
현재 AMR 이벤트는 ROS logger(stdout)로, 스모크 결과는 CLI의 JSON 한 줄로 출력됩니다.
